<table class="list">
  <tr>
    <th>ID</th>
    <th>Nome</th>
    <th>E-mail</th>
    <th>Remover?</th>
  </tr>

<% @newsletters.each do |newsletter| %>
  <tr>
  	<td align="center"><%= newsletter.id  %></td>
    <td><%= newsletter.nome  %></td>
    <td><%= newsletter.email  %></td>
	<td align="center"><%= link_to image_tag('icons/remove-icon.png'), "/administrator/newsletter/#{newsletter.id}",
               :confirm => 'Você tem certeza?',
               :method => :delete  %></td>
  </tr>
<% end %>
</table>

<%= pagination_footer @newsletters %>
